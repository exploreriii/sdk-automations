schemaVersion: 2
mode: active

capabilities:
  prDashboard:
    enabled: true
    checks:
      dcoSignoff:
        enabled: true
      mergeConflicts:
        enabled: true

principals:
  maintainerTeam: "hiero-ledger/hiero-sdk-python-maintainers"
