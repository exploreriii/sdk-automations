schemaVersion: 1
mode: active

capabilities:
  prQuality:
    enabled: true
    checks:
      dcoSignoff:
        enabled: true
      mergeConflicts:
        enabled: true

principals:
  maintainerTeam: "hiero-ledger/hiero-sdk-python-maintainers"
