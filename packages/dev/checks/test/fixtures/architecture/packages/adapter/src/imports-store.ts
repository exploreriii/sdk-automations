// adapter-imports-core-only: the adapter sits directly on core, and the one
// place holding credentials takes nothing from downstream of it.
export { note as storeNote } from "../../store/src/index.js";
