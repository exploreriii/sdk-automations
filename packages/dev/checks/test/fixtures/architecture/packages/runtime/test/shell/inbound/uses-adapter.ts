// adapter-imported-at-shell-main-only: the exemption is the sweep's directory
// alone, so a sibling lane's test reaching the same fake still fires.
export { harnessNote } from "../../adapter/harness.js";
