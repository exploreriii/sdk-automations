// not-to-unresolvable: the exports maps expose exactly ".", so a named
// subpath resolves to nothing and would otherwise produce no edge at all.
export * from "@hiero-hackers/automation-core/private";
