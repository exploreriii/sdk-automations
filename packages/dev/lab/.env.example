# The sandbox App's webhook secret (App settings → Webhook secret).
# Personal sandbox only — never a Hiero org App's. See lab/README.md.
WEBHOOK_SECRET=replace-me-not-a-real-secret

# Port the capture receiver listens on. Optional.
PORT=8788
