# 8.5 The prDashboard dashboard on a real pull request

The first armed run of prDashboard with every check on, against the personal sandbox,
on 2026-09-16: five deliveries on one pull request, one on a conflicting second, a
hand edit, and the first sweep firing.

## Question

That the five checks read GitHub's real shapes into one dashboard, that the dashboard
is updated in place as the pull request changes, that a redelivery duplicates nothing,
and how often `mergeable` is still `null` when the `synchronize` delivery arrives —
the measurement behind D198's open note.

## Red line

The lead's sandbox only; the pull requests the lead opened for the purpose; the
configuration enabling `prDashboard` in `active` put on the default branch by the
lead's own credentials for the run and restored after. Comment and label writes, on
the two fixtures only. The store is a fresh file under the scratch directory.

## Method

The shell from the branch under test with `APP_SLUG` set (writes armed) and no sweep
cadence, behind the smee relay 8.1 accepted; `TICK_SECONDS=20`.

| Fixture | State | Made by |
|---|---|---|
| A | one commit, no sign-off, no signature; no issue referenced | the lead |
| A′ | A with `Closes #n` added to the body, `n` assigned to someone else | the lead |
| A″ | A′ force-pushed with the commit signed off; `n` assigned to the lead | the lead |
| B | a branch conflicting with the default branch | the lead |
| `automations.yml` | `mode: active`, every check on; `applyLabels` added, then the GPG check parked, as the steps say | the lead, on the default branch |

Steps, each a `pull_request` delivery: open A; edit A′; push A″; open B; redeliver
one `synchronize` from the App's ledger; edit the dashboard by hand and push a no-op
commit. Then restart with `SWEEP_CADENCE_HOURS=1`, push to the default branch so B
stops conflicting, and read the next firing's re-evaluation of B (phase 3) and what
it charged. Reversal: close both, delete both branches, restore the configuration.

## Evidence

| Step | Delivery | Rows | `mergeable` at first read | Write |
|---|---|---|---|---|
| open A (#189), redelivered `479d19b0…` at 05:48Z | accepted, claimed, decided | — | — | none: `humanOrderingUnknown` — the timeline's one `committed` entry read as unreadable (D201) |
| the same redelivery at 05:50Z, same store | `deliveryDuplicate` | — | — | none — the GUID was already in the store |
| the same redelivery at 05:53Z, fresh store, reader fixed | decided in 8 s | DCO ❌ · GPG ❌ · merge ✅ · link ❌ · assignment ❌ | `true` (the event was 69 min old) | comment created 05:53:37Z; the App's own comment came back as an `issue_comment` delivery, decided without action |
| A′: body gains `Closes #188` at 05:54Z | `edited`, decided in 5 s | link ✅ #188 · assignment ❌ (#188 unassigned) | `true` | the one comment updated in place |
| A″: #188 assigned to the lead (no pull request event), then the commit force-pushed with a sign-off at 05:55Z | `synchronize`, decided in 5 s | DCO ✅ · GPG ❌ · merge ✅ · link ✅ · assignment ✅ | `true` at the first read, about 5 s after the push | updated in place; the assignment made on the issue was seen on the next pull request event |
| `applyLabels: [needsRevision, needsReview]` put on the default branch; title edited at 05:58Z | `edited`, decided in 10 s | as A″ | `true` | dashboard `already`; `status: needs revision` added — the label did not exist and GitHub created it on the add (a `label` delivery followed), answering 6.11's third question |
| GPG check parked in the configuration; title edited at 05:59Z | `edited`, decided in 4 s | DCO ✅ · merge ✅ · link ✅ · assignment ✅ — the parked row absent | `true` | `status: needs review` set along `revisionResolved`; `status: needs revision` removed with it (D4); the closing line reads all clear |
| open B (#190) at 06:00:07Z, conflicting with the default branch | `opened`, decided in 4 s | DCO ✅ · merge ❌ · link ❌ · assignment ❌ (nothing linked) | `false` at the first read, 4 s after opening | comment created; `status: needs revision` set from the null position along `checksFailed` |
| the lead edits B's dashboard by hand at 06:01:31Z, then edits the title | `edited`, decided in 6 s, the verdict unchanged | as before | `false` | the dashboard rewritten at 06:01:58Z: a managed body is the platform's (D145), so the hand edit did not survive |
| the default branch reverts its side of the conflict at 06:03Z; no pull request event; the shell restarted with `SWEEP_CADENCE_HOURS=1` | the sweep row (due since the first delivery) claimed at 06:04:38Z; 173 items decided in 161 s, 230 requests, 12 writes, 0 held back | B: merge ✅ · link ❌ · assignment ❌ | `null` for GitHub at 06:03Z, `true` by the sweep's read | B's dashboard rewritten at 06:07:18Z by the sweep lane; `needsRevision` stands (nothing linked); the six 6.9 fixture pull requests earned dashboards and labels of their own |

## What this run found

- Every enabled check reads GitHub's real shapes into one dashboard, updated in place
  by delivery and by sweep, one comment per pull request throughout; a redelivery on a
  store that has seen the GUID is a `deliveryDuplicate` with zero writes.
- **`mergeable` was never `null` at the moment a check read it**: six reads on two pull
  requests, including one 4 s after opening a conflicting branch and one after a
  force-push; GitHub reported `null` only in the seconds after the base moved, and the
  sweep's read minutes later found `true`. D198's open note can rest until a real
  repository says otherwise.
- The label path: a mapped label the repository lacks is created by GitHub on the add,
  grey (6.11's third question); `needsRevision` from the null position, `needsReview`
  along `revisionResolved` with the previous label removed, and `needsRevision` again on
  a fresh failure, all as the map draws them.
- An assignment made on the issue is seen on the next pull request event or sweep,
  which is what the schedule trigger is for (D200).
- Two timeline shapes the ordering reader could not read, `committed` and `reviewed`,
  each of which silenced every write on a pull request carrying one (D201). Until this
  run no armed write had been made on a pull request since #167.
- A hand edit to the dashboard does not survive the next evaluation (D145); the design
  page now says so.
- One sweep over 173 items, eight of them pull requests with every check on: 230
  requests and 161 s, before the read side's snapshot.

## What migrated

`packages/runtime/src/adapter/reads/externals.ts` (the two shapes), D201,
`packages/capabilities/src/prDashboard/design.md` (the hand-edit row, the assignment
wording, the `readyToMerge` guard the review of this run prompted).

## Notes

- Personal sandbox only (P8, D22). The run scripts are throwaway and outside the
  repository. Reversal: both fixtures closed and their branches deleted; the
  configuration restored; the two labels GitHub created, and the dashboards the sweep
  left on the six 6.9 fixtures, stand.
- The run starts from a redelivery of #189's `opened`, the App's ledger being the
  replay for a delivery made before the shell was listening.
- The first decided delivery wrote nothing: the ordering guard read the pull
  request's `committed` entry as unreadable. Fixed in the reader (D201); the run
  resumed from the same redelivery.
