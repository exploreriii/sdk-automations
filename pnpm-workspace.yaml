# Entries are listed rather than globbed (`packages/*`): the checks package
# reads this file as the one source of truth for what a package IS, and a
# glob would hand it a pattern where it needs paths.
packages:
    - packages/core
    - packages/store
    - packages/shell
    - packages/probes
    - packages/dev/checks
    - packages/dev/lab
    - packages/dev/testkit
